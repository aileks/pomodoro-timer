# Plan: New timer render + desert sunset theme

## Goals
- Switch timer to soft block rendering method
- Apply desert sunset color scheme
- Remove panel borders for a cleaner vibe

## Approach
1. Renderer refactor (soft block digits)
   - Replace 7‑segment logic in `cmd/pomodoro/glyphs.go` with bitmap digits.
   - Update `cmd/pomodoro/renderer.go` sizing math to match new glyph grid.
   - Keep responsive scaling based on available width.
2. Theme refresh
   - Update palette in `cmd/pomodoro/theme.go` to desert sunset colors.
   - Remove panel borders and adjust padding if needed.
   - Rework accent colors for work/short break/long break phases.
3. View tuning
   - Adjust `availableWidth` to align with new padding (no border).
   - Ensure header/timer contrast remains strong.
4. Tests
   - Regenerate renderer golden snapshots in `cmd/pomodoro/testdata/`.

## Files to change
- `cmd/pomodoro/glyphs.go`
- `cmd/pomodoro/renderer.go`
- `cmd/pomodoro/theme.go`
- `cmd/pomodoro/view.go`
- `cmd/pomodoro/testdata/renderer_scale*.golden`

## Verification
- `UPDATE_GOLDEN=1 go test ./cmd/pomodoro -run TestRendererGolden`
- `go test ./...`
- `go run ./cmd/pomodoro` and visually confirm new vibe
